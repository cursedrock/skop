[build]
  builder = "nixpacks"
  buildCommand = "pip install -r requirements.txt"

[deploy]
  startCommand = "gunicorn app:app"
  healthcheckPath = "/"

[variables]
  PORT = "5000"
  FLASK_ENV = "production"